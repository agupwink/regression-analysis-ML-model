# regression-analysis-ML-model
Building 3 ML models - Linear regression, decision tree, random forest on a dataset taken from kaggle. Calculating there accuracy, MSE and MAE. Plotting them visually using a barplot, histogram, line chart and heatmap(correlation).
